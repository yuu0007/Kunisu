import { Client, GuildChannel } from 'discord.js';
/**
 * @handler Channel Events
 * @related channelUpdate
 */
export declare function handleChannelUpdateEvent(client: Client, oldChannel: GuildChannel, newChannel: GuildChannel): Promise<void>;
